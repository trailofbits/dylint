[package]
name = "examples"
version = "2.1.11"
description = "A dummy package for testing the example Dylint libraries"
edition = "2021"
license = "MIT OR Apache-2.0"
publish = false

[dev-dependencies]
cargo_metadata = "0.15"
toml_edit = "0.19"
walkdir = "2.3"

dylint_internal = { version = "=2.1.11", path = "../internal", features = ["clippy_utils", "examples"] }
